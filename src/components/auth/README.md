
# Authentication Components

This directory contains components related to user authentication such as sign-in and sign-up forms.

Key files:
- `AuthContainer.tsx`: Container component for authentication forms
- `SignInForm.tsx`: Form component for user sign-in
- `SignUpForm.tsx`: Form component for user registration
