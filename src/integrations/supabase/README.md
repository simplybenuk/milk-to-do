
# Supabase Integration

This directory contains files related to the Supabase integration, which provides backend functionality for SourList.

Key files:
- `client.ts`: Initializes and exports the Supabase client for use throughout the application
- `types.ts`: TypeScript type definitions for Supabase database schema
