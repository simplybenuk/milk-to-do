
# Stores Directory

This directory contains state management logic using Zustand for the SourList application.

Key stores:
- `useTaskStore.ts`: Manages task state throughout the application
- `useTagStore.ts`: Manages tag state

The directory also includes:
- `actions/`: Action creators for updating state
- `utils/`: Utility functions for state management
- `types/`: TypeScript type definitions for stores
