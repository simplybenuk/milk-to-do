
# Context Directory

This directory contains React context providers that make values available to components throughout the component tree without having to pass props down manually.

Key contexts:
- `PostHogProvider.tsx`: Provides PostHog analytics functionality to components
